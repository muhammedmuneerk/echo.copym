import React from "react";

const Buttons = () => {
  return (
    <div>
      <button class="bg-gradient-to-r from-green-400 to-blue-500 text-white font-semibold px-8 py-3 rounded-full shadow-lg transition-all duration-300 ease-in-out transform hover:scale-105 hover:shadow-2xl focus:outline-none focus:ring-4 focus:ring-blue-300">
        buttons
      </button>
    </div>
  );
};

export default Buttons;
